#include "Operations.h"

/*void filterGymExercises(GymExercise exercitii[], int n, int noOfseries, int noOfReps, GymExercise result[], int& len, float x)
{
    len = 0;
    for (int i = 0; i < n; i++)
    {
        if ((exercitii[i].getNoOfSeries() * exercitii[i].getNoOfReps() * exercitii[i].getWeightKg()) > x)

            result[len++] = exercitii[i];

    }
}
void filterGymExercisesWithRepo(Repo repo, int n, int noOfseries, int noOfReps, GymExercise result[], int& len, float x)
{
    len = 0;
    GymExercise* initialExercises = repo.getAll();
    return filterGymExercises(initialExercises,n, noOfseries, noOfReps, result, len, x);

}
void filetr(int&len,int n,float x,GymExercise* result)
{
    Repo repo;
    GymExercise* exercitii = repo.getAll();
   
    len = 0;
    for (int i = 0; i < n; i++)
    {
        if ((exercitii[i].getNoOfSeries() * exercitii[i].getNoOfReps() * exercitii[i].getWeightKg()) > x)

            result[len++] = exercitii[i];

    }

}*/



